import EditTagLineTextCell from 'src/components/TagLineText/EditTagLineTextCell';

const EditTagLineTextPage = ({ id }) => {
  return <EditTagLineTextCell id={id} />;
};

export default EditTagLineTextPage;
