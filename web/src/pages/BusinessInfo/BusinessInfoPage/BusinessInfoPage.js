import BusinessInfoCell from 'src/components/Footer/BusinessInfo/BusinessInfoCell';

const BusinessInfoPage = ({ id }) => {
  return <BusinessInfoCell id={id} />;
};

export default BusinessInfoPage;
