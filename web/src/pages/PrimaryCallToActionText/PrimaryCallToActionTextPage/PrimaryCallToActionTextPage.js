import PrimaryCallToActionTextCell from 'src/components/PrimaryCallToActionText/PrimaryCallToActionTextCell';

const PrimaryCallToActionTextPage = ({ id }) => {
  return <PrimaryCallToActionTextCell id={id} />;
};

export default PrimaryCallToActionTextPage;
