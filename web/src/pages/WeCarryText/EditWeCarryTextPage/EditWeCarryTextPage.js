import EditWeCarryTextCell from 'src/components/WeCarryText/EditWeCarryTextCell';

const EditWeCarryTextPage = ({ id }) => {
  return <EditWeCarryTextCell id={id} />;
};

export default EditWeCarryTextPage;
