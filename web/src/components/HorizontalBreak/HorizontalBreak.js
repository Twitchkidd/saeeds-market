import styled from 'styled-components';
import { blue200 } from 'src/utils/colors';

const HorizontalBreak = styled.div`
  width: 100%;
  height: 2px;
  border-radius: 1px;
  background: ${blue200};
`;

export default HorizontalBreak;
